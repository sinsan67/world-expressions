Lance `python3 scripts/validate_db.py` depuis la racine du projet et affiche le résultat.

Si des erreurs sont détectées, explique ce qu'elles signifient concrètement et propose les commandes exactes pour les corriger.

Si JSON et SQLite sont désynchronisés, propose de relancer la migration : `python3 scripts/migrate_json_to_sqlite.py`.
