# Claude Code local settings (ne pas commiter — propres à la machine)
.claude/settings.json
.claude/settings.local.json
# .claude/commands/ est versionné : les skills sont propres au projet

# Python
__pycache__/
*.pyc
*.pyo
.env.dev
.env.prod
venv/
.venv/

# macOS
.DS_Store

# Node
node_modules/

# Database (fichier généré — source de vérité = data/expressions.json)
data/*.db

# Raw data (fichiers sources lourds, non versionnés)
data/raw/

# Logs
*.log

# SQLite local (source de vérité = Neon)
*.db

# Test artifacts
web/playwright-report/
web/test-results/
test-results/
nao/.env

# Ressources locales (audios, transcriptions perso — non destinés au repo public)
ressources/
